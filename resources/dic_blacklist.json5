{
  blacklist: [
    {
      key: "^[a-z0-9]{1}$",
      reason: "半角英数1文字は意図しない一致をするため（デフォルトの制限）"
    },
    {
      key: "^[0-9]{2}$",
      reason: "数字2文字は意図しない一致をするため（デフォルトの制限）"
    },
    {
      key: "^[?？]+$",
      reason: "はてなだけの登録は意図しない一致をするため（デフォルトの制限）"
    }
  ]
}
